[project]
name = "ci-runner"
version = "0.1.0"
description = "In-cluster build/test/push/deploy runner for cluster-ci"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "gunicorn>=26.0.0",
    "kubernetes>=31.0.0",
]
